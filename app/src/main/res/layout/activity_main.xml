<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Top bar -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:padding="12dp">

        <!-- Settings icon -->
        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_manage" />

        <!-- Title -->
        <TextView
            android:id="@+id/txtTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Food Management"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Search icon -->
        <ImageButton
            android:id="@+id/btnSearch"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_search" />
    </RelativeLayout>

    <!-- Alerts section -->
    <LinearLayout
        android:id="@+id/layoutAlerts"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Alerts"
            android:textStyle="bold"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/txtAlertsList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No alerts."
            android:textSize="14sp"
            android:textColor="#808080"
            android:paddingTop="4dp" />
    </LinearLayout>

    <!-- Category chips -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <!-- Each button is one category -->
            <Button
                android:id="@+id/btnCatAll"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="All" />

            <Button
                android:id="@+id/btnCatVeg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Veg"
                android:layout_marginStart="8dp" />

            <Button
                android:id="@+id/btnCatMeat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Meat"
                android:layout_marginStart="8dp" />

            <Button
                android:id="@+id/btnCatFruits"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Fruits"
                android:layout_marginStart="8dp" />

            <Button
                android:id="@+id/btnCatDrinks"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Drinks"
                android:layout_marginStart="8dp" />

            <Button
                android:id="@+id/btnCatSeason"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Seasonings"
                android:layout_marginStart="8dp" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Food list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerFoods"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp" />

    <!-- Add food button -->
    <Button
        android:id="@+id/btnAddFood"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="+  Add Food"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:background="#00A8A8"
        android:textColor="#FFFFFF"
        android:layout_margin="16dp"
        android:layout_marginBottom="24dp" />

</LinearLayout>
