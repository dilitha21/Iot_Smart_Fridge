<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartfridge">

    <!-- Internet permissions for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fridge IoT Monitor"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Dashboard (Main Screen) -->
        <activity
            android:name="com.example.smartfridge.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add Food Screen -->
        <activity
            android:name="com.example.smartfridge.AddFoodActivity"
            android:exported="false" />

        <!-- Search Screen -->
        <activity
            android:name="com.example.smartfridge.SearchActivity"
            android:exported="false" />

        <!-- Settings Screen -->
        <activity
            android:name="com.example.smartfridge.SettingsActivity"
            android:exported="false" />

    </application>

</manifest>
